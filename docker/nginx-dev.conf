events {
    worker_connections 1024;
}

http {
    upstream usmanramzan-ai {
        server host.docker.internal:8001;
    }
    
    upstream urnlabs-ai {
        server host.docker.internal:8002;
    }
    
    upstream eprecisio-com {
        server host.docker.internal:8003;
    }

    server {
        listen 80;
        server_name usmanramzan.local;
        
        location / {
            proxy_pass http://usmanramzan-ai;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
    
    server {
        listen 80;
        server_name urnlabs.local;
        
        location / {
            proxy_pass http://urnlabs-ai;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
    
    server {
        listen 80;
        server_name eprecisio.local;
        
        location / {
            proxy_pass http://eprecisio-com;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
    
    server {
        listen 80 default_server;
        
        location / {
            return 200 '
<!DOCTYPE html>
<html>
<head>
    <title>UrnLabs Development Server</title>
    <style>
        body { font-family: Arial, sans-serif; padding: 40px; background: #0f172a; color: #fff; }
        .container { max-width: 800px; margin: 0 auto; }
        .site-links { display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 20px; margin-top: 40px; }
        .site-link { background: rgba(255,255,255,0.1); padding: 20px; border-radius: 8px; text-decoration: none; color: inherit; border: 1px solid rgba(255,255,255,0.2); }
        .site-link:hover { background: rgba(255,255,255,0.15); }
    </style>
</head>
<body>
    <div class="container">
        <h1>🚀 UrnLabs Development Environment</h1>
        <p>Multi-site development server running with Docker Compose</p>
        
        <div class="site-links">
            <a href="http://localhost:8001" class="site-link">
                <h3>UsmanRamzan.ai</h3>
                <p>Personal AI Portfolio</p>
                <small>localhost:8001</small>
            </a>
            <a href="http://localhost:8002" class="site-link">
                <h3>UrnLabs.ai</h3>
                <p>AI Agent Platform</p>
                <small>localhost:8002</small>
            </a>
            <a href="http://localhost:8003" class="site-link">
                <h3>EPrecisio.com</h3>
                <p>DevOps Excellence</p>
                <small>localhost:8003</small>
            </a>
        </div>
        
        <div style="margin-top: 40px; padding: 20px; background: rgba(0,255,0,0.1); border-radius: 8px;">
            <h3>🐳 Docker Compose Commands</h3>
            <code>docker-compose -f docker-compose.dev.yml up -d</code><br>
            <code>docker-compose -f docker-compose.dev.yml logs -f</code><br>
            <code>docker-compose -f docker-compose.dev.yml down</code>
        </div>
    </div>
</body>
</html>
            ';
            add_header Content-Type text/html;
        }
    }
}