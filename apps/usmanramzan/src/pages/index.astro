---
/**
 * Modern CTO Portfolio Homepage
 * Complete redesign with glassmorphism aesthetics and professional layout
 * Integrates all custom components and enhanced theme system
 */

import BaseLayout from '~/layouts/BaseLayout.astro';
import Navigation from '~/components/Navigation.astro';
import ExpertiseCard from '~/components/ExpertiseCard.astro';
import ProjectCard from '~/components/ProjectCard.astro';
import ContactForm from '~/components/ContactForm.astro';

// Portfolio data - Production ready content
const expertiseAreas = [
  {
    icon: 'brain',
    title: 'AI Architecture',
    description: 'Designing scalable AI systems with autonomous agents, decision-making frameworks, and governance-first approaches for enterprise deployment.'
  },
  {
    icon: 'git-branch',
    title: 'ML Engineering',
    description: 'Building production-ready machine learning pipelines with MLOps best practices, automated deployment, and comprehensive monitoring.'
  },
  {
    icon: 'network',
    title: 'Agent Systems',
    description: 'Developing collaborative AI agents with emergent behavior patterns, multi-agent orchestration, and deterministic workflow execution.'
  }
];

const featuredProjects = [
  {
    title: 'Autonomous Trading Platform',
    description: 'Multi-agent system for automated market analysis and trading execution with real-time risk management and compliance monitoring.',
    imageUrl: 'https://images.unsplash.com/photo-1620712943543-bcc4688e7485?w=600&h=400&fit=crop',
    imageAlt: 'AI Trading Platform Dashboard',
    caseStudyUrl: '/projects/trading-platform',
    technologies: ['Python', 'TensorFlow', 'Kubernetes', 'PostgreSQL', 'Redis'],
    category: 'AI/ML'
  },
  {
    title: 'Enterprise MLOps Platform',
    description: 'Scalable machine learning operations platform with automated deployment pipelines, model versioning, and comprehensive observability.',
    imageUrl: 'https://images.unsplash.com/photo-1555949963-ff9fe0c870eb?w=600&h=400&fit=crop',
    imageAlt: 'MLOps Platform Architecture',
    caseStudyUrl: '/projects/mlops-platform',
    technologies: ['Kubernetes', 'Kubeflow', 'Docker', 'Prometheus', 'Grafana'],
    category: 'DevOps'
  }
];

const latestInsights = [
  {
    date: 'March 15, 2024',
    title: 'The Evolution of AI Agents',
    excerpt: 'Exploring how self-improving AI systems are reshaping enterprise automation and the governance challenges they present.',
    url: '/insights/ai-agents-evolution',
    category: 'AI'
  },
  {
    date: 'March 10, 2024',
    title: 'Scaling ML Infrastructure',
    excerpt: 'Best practices for building robust, scalable machine learning infrastructure that grows with your organization.',
    url: '/insights/scaling-ml-infrastructure',
    category: 'MLOps'
  },
  {
    date: 'March 5, 2024',
    title: 'Agent Collaboration Patterns',
    excerpt: 'Design patterns and architectural approaches for implementing effective multi-agent systems in production.',
    url: '/insights/agent-collaboration-patterns',
    category: 'Architecture'
  }
];
---

<BaseLayout
  title="Muhammad Usman Ramzan - AI & ML Engineering CTO"
  description="Senior AI/ML Engineering CTO and DevOps specialist. Building production-ready AI agent platforms, ML infrastructure, and governance-first automation systems."
>
  <!-- Enhanced CSS Integration -->
  <link rel="stylesheet" href="/cto_theme.css">
  
  <!-- Lucide Icons -->
  <script src="https://unpkg.com/lucide@latest/dist/umd/lucide.min.js"></script>
  
  <!-- Google Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@200;300;400;500;600;700&display=swap" rel="stylesheet">

  <!-- Fixed Navigation -->
  <Navigation currentPath="/" />

  <main class="min-h-screen overflow-hidden">
    <!-- Hero Section with Glassmorphism and Geometric Patterns -->
    <section id="hero" class="relative min-h-screen flex items-center pt-20 bg-gradient-to-br from-gray-50 to-gray-100">
      <!-- Background Patterns -->
      <div class="absolute inset-0">
        <div class="absolute inset-0" style="background-image: radial-gradient(#e5e5e5 1px, transparent 1px); background-size: 30px 30px; opacity: 0.3;"></div>
        <div class="absolute top-10 left-1/4 w-72 h-72 rounded-full bg-gradient-to-br from-gray-100 to-gray-200 opacity-20 animate-pulse"></div>
        <div class="absolute bottom-20 right-1/3 w-60 h-60 rounded-full bg-gradient-to-br from-gray-200 to-gray-300 opacity-15 animate-pulse" style="animation-delay: 1s;"></div>
        <div class="absolute top-1/2 right-1/4 w-80 h-80 rounded-full bg-gradient-to-br from-gray-50 to-gray-150 opacity-10 animate-pulse" style="animation-delay: 2s;"></div>
      </div>

      <div class="relative z-10 max-w-7xl mx-auto px-6 py-16 lg:py-24">
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
          <!-- Hero Content -->
          <div class="space-y-8">
            <h1 class="text-4xl md:text-5xl lg:text-6xl font-light leading-tight mb-6" style="background: linear-gradient(135deg, #1a1a1a 0%, #666666 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;">
              Shaping the Future of AI Automation
            </h1>
            
            <p class="text-xl text-gray-600 max-w-2xl leading-relaxed mb-8">
              Architecting intelligent systems that transform business operations through advanced AI agents, ML workflows, and governance-first infrastructure.
            </p>

            <!-- CTA Buttons -->
            <div class="flex flex-col sm:flex-row gap-4 pt-4 cto-animate-fade-up cto-animate-delay-200">
              <a href="#contact" class="inline-flex items-center justify-center px-8 py-3 bg-gray-900 text-white font-medium rounded-full hover:bg-gray-800 transition-all duration-300 hover:-translate-y-1">
                Start a Conversation
                <i data-lucide="arrow-right" class="ml-2 w-4 h-4"></i>
              </a>
              <a href="#projects" class="inline-flex items-center text-gray-900 font-medium hover:text-gray-600 transition-colors duration-300">
                View Projects
                <i data-lucide="arrow-right" class="ml-2 w-4 h-4"></i>
              </a>
            </div>
          </div>

          <!-- Hero Visual -->
          <div class="relative lg:justify-self-end">
            <div class="relative w-full max-w-md mx-auto">
              <!-- Geometric Visual Container -->
              <div class="aspect-square bg-gradient-to-br from-gray-50 to-gray-100 rounded-3xl overflow-hidden relative">
                <div class="absolute inset-0 opacity-20" style="background-image: linear-gradient(rgba(229, 229, 229, 0.3) 1px, transparent 1px), linear-gradient(90deg, rgba(229, 229, 229, 0.3) 1px, transparent 1px); background-size: 20px 20px;"></div>
                <div class="absolute inset-4 bg-gradient-to-br from-transparent via-white/10 to-white/5 rounded-2xl backdrop-blur-sm"></div>
                
                <!-- Tech Icons Floating -->
                <div class="absolute inset-0 flex items-center justify-center">
                  <div class="grid grid-cols-3 gap-6 p-8">
                    <div class="w-12 h-12 bg-gradient-to-br from-blue-500 to-purple-600 rounded-xl flex items-center justify-center text-white cto-animate-float-gentle">
                      <i data-lucide="cpu" class="w-6 h-6"></i>
                    </div>
                    <div class="w-12 h-12 bg-gradient-to-br from-green-500 to-teal-600 rounded-xl flex items-center justify-center text-white cto-animate-float-gentle" style="animation-delay: 0.5s;">
                      <i data-lucide="database" class="w-6 h-6"></i>
                    </div>
                    <div class="w-12 h-12 bg-gradient-to-br from-orange-500 to-red-600 rounded-xl flex items-center justify-center text-white cto-animate-float-gentle" style="animation-delay: 1s;">
                      <i data-lucide="cloud" class="w-6 h-6"></i>
                    </div>
                    <div class="w-12 h-12 bg-gradient-to-br from-purple-500 to-pink-600 rounded-xl flex items-center justify-center text-white cto-animate-float-gentle" style="animation-delay: 1.5s;">
                      <i data-lucide="activity" class="w-6 h-6"></i>
                    </div>
                    <div class="w-12 h-12 bg-gradient-to-br from-indigo-500 to-blue-600 rounded-xl flex items-center justify-center text-white cto-animate-float-gentle" style="animation-delay: 2s;">
                      <i data-lucide="zap" class="w-6 h-6"></i>
                    </div>
                    <div class="w-12 h-12 bg-gradient-to-br from-teal-500 to-green-600 rounded-xl flex items-center justify-center text-white cto-animate-float-gentle" style="animation-delay: 2.5s;">
                      <i data-lucide="shield" class="w-6 h-6"></i>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Scroll Indicator -->
      <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 text-gray-600 animate-bounce">
        <i data-lucide="chevron-down" class="w-6 h-6"></i>
      </div>
    </section>

    <!-- Expertise Section -->
    <section id="expertise" class="py-20 bg-cto-secondary-bg">
      <div class="max-w-7xl mx-auto px-6">
        <h2 class="text-3xl font-light text-center mb-16 text-gray-900">
          Areas of Expertise
        </h2>
        
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
          {expertiseAreas.map((area, index) => (
            <ExpertiseCard
              icon={area.icon}
              title={area.title}
              description={area.description}
              index={index}
            />
          ))}
        </div>
      </div>
    </section>

    <!-- Featured Projects Section -->
    <section id="projects" class="py-20 bg-cto-accent-bg">
      <div class="max-w-7xl mx-auto px-6">
        <h2 class="text-3xl font-light text-center mb-16 text-gray-900">
          Featured Work
        </h2>
        
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 max-w-6xl mx-auto">
          {featuredProjects.map((project, index) => (
            <ProjectCard
              title={project.title}
              description={project.description}
              imageUrl={project.imageUrl}
              imageAlt={project.imageAlt}
              caseStudyUrl={project.caseStudyUrl}
              technologies={project.technologies}
              category={project.category}
              index={index}
            />
          ))}
        </div>
        
        <!-- View All Projects Link -->
        <div class="text-center mt-12">
          <a href="/projects" class="inline-flex items-center text-lg font-medium text-gray-900 hover:text-gray-600 transition-colors">
            View All Projects
            <i data-lucide="arrow-right" class="w-5 h-5"></i>
          </a>
        </div>
      </div>
    </section>

    <!-- Latest Insights Section -->
    <section id="insights" class="py-20 bg-cto-secondary-bg">
      <div class="max-w-7xl mx-auto px-6">
        <h2 class="text-3xl font-light text-center mb-16 text-gray-900">
          Latest Insights
        </h2>
        
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 max-w-6xl mx-auto">
          {latestInsights.map((insight, index) => (
            <article class="p-6 bg-white bg-opacity-85 backdrop-blur-sm border border-white border-opacity-20 rounded-xl shadow-sm hover:transform hover:-translate-y-2 transition-all duration-300 hover:shadow-lg" data-animate="fade-up" style={`animation-delay: ${index * 100}ms`}>
              <div class="space-y-4">
                <div class="flex items-center justify-between">
                  <time class="text-sm text-gray-500">
                    {insight.date}
                  </time>
                  <span class="px-2 py-1 bg-gray-100 text-gray-700 text-xs rounded-full font-medium">
                    {insight.category}
                  </span>
                </div>
                
                <h3 class="text-xl font-semibold text-gray-900">
                  <a href={insight.url} class="hover:text-gray-600 transition-colors">
                    {insight.title}
                  </a>
                </h3>
                
                <p class="text-gray-600">
                  {insight.excerpt}
                </p>
                
                <a href={insight.url} class="inline-flex items-center text-sm font-medium text-gray-900 hover:text-gray-600 transition-colors">
                  Read More
                  <i data-lucide="arrow-right" class="w-4 h-4"></i>
                </a>
              </div>
            </article>
          ))}
        </div>
        
        <!-- View All Insights Link -->
        <div class="text-center mt-12">
          <a href="/insights" class="inline-flex items-center text-lg font-medium text-gray-900 hover:text-gray-600 transition-colors">
            View All Insights
            <i data-lucide="arrow-right" class="w-5 h-5"></i>
          </a>
        </div>
      </div>
    </section>

    <!-- Contact Section -->
    <section id="contact" class="bg-cto-accent-bg">
      <ContactForm />
    </section>
  </main>

  <!-- Enhanced JavaScript -->
  <script>
    // Initialize Lucide icons
    if (typeof lucide !== 'undefined') {
      lucide.createIcons();
    }

    // Enhanced scroll-triggered animations
    class PortfolioAnimationController {
      constructor() {
        this.animatedElements = document.querySelectorAll('[data-animate]');
        this.init();
      }

      init() {
        this.setupIntersectionObserver();
        this.setupSmoothScrolling();
        this.setupPerformanceOptimizations();
      }

      setupIntersectionObserver() {
        const observer = new IntersectionObserver(
          (entries) => {
            entries.forEach((entry) => {
              if (entry.isIntersecting) {
                const animationType = entry.target.getAttribute('data-animate');
                entry.target.classList.add(`cto-animate-${animationType}`);
                observer.unobserve(entry.target);
              }
            });
          },
          {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
          }
        );

        this.animatedElements.forEach((el) => {
          observer.observe(el);
        });
      }

      setupSmoothScrolling() {
        // Enhanced smooth scrolling for anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
          anchor.addEventListener('click', function (e) {
            e.preventDefault();
            const target = document.querySelector(this.getAttribute('href'));
            if (target) {
              const offsetTop = target.offsetTop - 80;
              window.scrollTo({
                top: offsetTop,
                behavior: 'smooth'
              });
            }
          });
        });
      }

      setupPerformanceOptimizations() {
        // Reduce motion for users who prefer it
        const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
        
        if (prefersReducedMotion) {
          document.documentElement.style.setProperty('--cto-transition-fast', '0s');
          document.documentElement.style.setProperty('--cto-transition-normal', '0s');
          document.documentElement.style.setProperty('--cto-transition-slow', '0s');
        }
      }
    }

    // Initialize animation controller when DOM is ready
    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', () => {
        new PortfolioAnimationController();
      });
    } else {
      new PortfolioAnimationController();
    }
  </script>
</BaseLayout>